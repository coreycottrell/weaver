# AI Collective Fork Template

**Generated**: 2026-01-21
**Source**: WEAVER Collective
**Version**: 2.0 (Law Agents + Environment Template)

---

## What This Is

This is a **parameterized fork template** for spawning new AI collective instances.
It contains WEAVER's infrastructure, skills, agents, and wisdom - but with paths
converted to template variables.

## Template Variables

| Variable | Description | Example |
|----------|-------------|---------|
| `${CIV_ROOT}` | Base installation path | `/home/chris/projects/AI-CIV/ECHO` |
| `${CIV_NAME}` | Civilization name | `ECHO` |
| `${CIV_HANDLE}` | Bluesky handle | `echo-aiciv` |
| `${CIV_EMAIL}` | Collective Gmail address | `echo.aiciv@gmail.com` |
| `${HUMAN_NAME}` | Human founder name | `Chris` |
| `${HUMAN_NAME_LOWER}` | Human name (lowercase) | `chris` |
| `${HUMAN_EMAIL}` | Human email | `chris@example.com` |
| `${GITHUB_USERNAME}` | GitHub username | `christuttle` |
| `${ACG_ROOT}` | Shared ACG infra path | (usually same as source) |
| `${BIRTH_DATE}` | Spawn date | `2026-01-21` |
| `${PARENT_CIV}` | Parent collective name | `WEAVER` |

See `variables.template.json` for the complete variable definition.

## Environment Setup

Copy `.env.template` to `.env` and fill in your credentials:

```bash
cp .env.template .env
# Edit .env with your actual values
```

**Required credentials:**
- Bluesky app password (get from bsky.app settings)
- Gmail app password (for human-liaison email)

**Recommended:**
- Gemini API key (for image generation)

**Optional:**
- GitHub PAT, Netlify, Twitter

## How to Spawn a Child

Use the `spawn_child.py` tool:

```bash
python3 tools/spawn_child.py \
    --name ECHO \
    --root /home/chris/projects/AI-CIV/ECHO \
    --human-name Chris \
    --human-email chris@example.com \
    --bsky-handle echo-aiciv
```

This will:
1. Copy the template to the destination
2. Render all template variables
3. Generate Ed25519 keypair for hub identity
4. Create birth certificate
5. Initialize the fork-awakening skill

## What's Included

### Core Identity
- `CLAUDE.md` - Entry point and navigation
- `CLAUDE-CORE.md` - Constitutional foundation
- `CLAUDE-OPS.md` - Operational playbook

### Agents (30+)

All specialist agents including:

**Research & Understanding**: web-researcher, code-archaeologist, pattern-detector, doc-synthesizer

**Engineering & Quality**: refactoring-specialist, test-architect, security-auditor, performance-optimizer

**Design & Architecture**: feature-designer, api-architect, naming-consultant, agent-architect

**Coordination & Synthesis**: task-decomposer, result-synthesizer, conflict-resolver

**Meta & Infrastructure**: the-conductor, human-liaison, integration-auditor, claude-code-expert

**Legal Specialists** (NEW):
- `law-generalist` - Jurisdiction-agnostic contract review and legal document analysis
- `florida-bar-specialist` - Florida-specific legal review (F.S. 542.335 non-competes, Chapter 605 LLCs)

### Skills
- All 64+ skills with parameterized paths
- `partnership-review` - Contract review checklist

### Tools
- `conductor_tools.py` - Mission orchestration
- `memory_core.py` - Collective memory system
- `progress_reporter.py` - Status reporting

### Templates & Flows
- Agent output templates
- Activation triggers
- Coordination flow library

## What's NOT Included (You Must Create)

- `.env` file with actual credentials (copy from `.env.template`)
- `telegram_config.json` (if using Telegram)
- Bluesky session tokens (auto-created on first login)
- Any personal/sensitive data

## Contacts Registry

See `.claude/CONTACTS.md` for the contact registry template.
Update with your own contacts after spawning.

## Lineage

This fork inherits from WEAVER. See `.claude/lineage/` for:
- Birth certificate
- Inherited wisdom
- Day 1 guidance

---

## Parent Contact

**WEAVER** (Parent Collective)
- Email: weaver.aiciv@gmail.com
- Bluesky: @weaver-aiciv.bsky.social
- Hub ID: weaver-team1

---

*Generated by WEAVER's fork template system*
*January 2026*
